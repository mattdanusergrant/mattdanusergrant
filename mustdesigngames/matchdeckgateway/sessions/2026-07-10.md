# Match Deck Gateway — 2026-07-10

**Task:** Complete the PartyKit backend rename that was flagged pending after the
2026-07-09 Multiplayer Deck Gateway → Match Deck Gateway rename.

**Status:** Done.
- `party/partykit.json` project name → `matchdeckgateway`
- `party/package.json` name → `matchdeckgateway-party`, description updated
- `party/README.md` note rewritten + deploy/host examples updated to the new name
- `index.html` header NOTE + `PARTY_HOST` example comment updated
- No remaining `multiplayerdeckgateway` references anywhere in the project
- Smoke tests: 26/26 pass

**Decisions made:** Safe to rename now without a redeploy because `PARTY_HOST` is
empty — the client is on the PeerJS fallback and no room server is wired in, so the
old name has no live host to break. On the next `npm run deploy` the server publishes
as `matchdeckgateway.<user>.partykit.dev`; that host then gets set in `PARTY_HOST`.

**Blockers:** None. The actual `npm run deploy` (and wiring the printed host into
`PARTY_HOST`) is an Operator step — it needs PartyKit/Cloudflare auth and only matters
when switching online play off the PeerJS fallback.
